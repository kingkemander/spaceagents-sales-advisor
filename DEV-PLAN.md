# Development Plan — SA 销售军师

> 本文件记录“客户企业动态雷达”迭代的开发阶段、验收标准和剩余工作。

---

## Phase 1: 企业身份与外部证据库

**交付内容**：

- 创建客户企业身份记录，保存法定全称、统一社会信用代码、注册地区、曾用名、品牌和关联公司。
- 创建独立外部动态证据流水，保存来源、原文链接、发布日期、采集时间、主体匹配依据、核验状态和内容摘要。
- 实现代码级写入边界：缺少法定全称或统一社会信用代码、只有简称、只有搜索摘要或无法访问原文的记录，只能标记为待核验，不能写入正式客户时间线。

**关键文件**：

- `sa_sales_advisor/company_radar.py` — 企业身份、证据规范化、去重、主体匹配和核验状态管理。
- `sa_sales_advisor/cli.py` — 暴露 `company-radar` 命令入口。
- `playbooks/company-intelligence-radar/references/evidence-schema.md` — 企业动态证据字段、可信等级与写入规则。

**验收标准**：

- Python 编译无错误，现有测试不回归。
- 同名企业但统一社会信用代码不一致的记录不能标记为已核验。
- 搜索摘要可以保存为线索，但不能合并进客户时间线。
- 同一公告的重复 URL、公告标识或内容摘要只保留一条主记录。

---

## Phase 2: 公开信息检索与合规降级

**交付内容**：

- 根据企业身份生成招投标、采购、中标、备案、环评、土地、许可处罚、工商、融资、招聘、扩产、司法、知识产权和官方新闻的检索计划。
- 优先路由中国政府采购网、全国公共资源交易平台、中国招标投标公共服务平台、地方公共资源交易平台、信用中国、国家企业信用信息公示系统、政府网站和企业官网。
- 对允许公开访问的网页执行低频抓取、缓存、正文提取和内容哈希；遇到验证码、登录、403、429 或网站禁止自动访问时停止，不绕过限制，降级为待核验链接或浏览器人工核验。
- 支持将 SpaceAgents 可用的浏览器/搜索工具结果以结构化 JSON 导入证据库，使工具能力不同的安装环境使用同一核验标准。

**关键文件**：

- `sa_sales_advisor/company_radar.py` — 检索计划、公开页面读取、缓存、导入与错误降级。
- `playbooks/company-intelligence-radar/PLAYBOOK.md` — 智能体联网检索、浏览器核验、批量导入和销售确认流程。
- `playbooks/company-intelligence-radar/references/source-registry.md` — 官方数据源、查询字段、访问边界和降级路径。

**验收标准**：

- 输入法定全称和统一社会信用代码后可生成覆盖全部动态类型的查询计划。
- 403、429、验证码和登录页面不会触发重试轰炸或绕过行为。
- 每条已核验动态都有可访问原文、发布日期、采集时间和企业匹配依据。
- 没有网络或所有来源受限时返回明确的待核验清单，不虚构“未发现动态”。

---

## Phase 3: 客户卡片、行动触发与回复联动

**交付内容**：

- 在客户卡片增加“企业动态”区，展示最近三条高价值动态、企业角色、业务影响、可信等级和原文入口。
- 在今日行动增加“动态触发”标记，让新中标、采购、扩产、融资、处罚或诉讼变化进入销售优先级判断。
- 调用销售思想库分析动态与当前机会的可能关系，输出支持依据、反证、最小验证问题和自然跟进建议，并按 `sales-soul.md` 生成本人表达。
- 支持启动检查、每日一次本机检查和用户手动刷新；电脑关机时不承诺持续监控。

**关键文件**：

- `sa_sales_advisor/render_dashboard.py` — 合并已核验企业动态和动态触发状态。
- `sa_sales_advisor/templates/dashboard-template.html` — 企业动态卡片和筛选交互。
- `playbooks/draft-sales-reply/PLAYBOOK.md` — 把已核验企业动态纳入回复依据与方法匹配。
- `skills/sa-sales-advisor/SKILL.md` — 路由企业动态查询、更新、确认和回复联动。

**验收标准**：

- 客户卡片只显示已核验记录，待核验线索单独提示且不改变客户阶段。
- 每条建议能回到明确动态和当前客户事实，不把相关性推测写成事实。
- 页面不出现爬虫、模型、置信度和内部文件路径等技术措辞。
- 插件安装、客户材料入库、个人口吻、回复起草和语音提醒测试保持通过。

---

## 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 插件运行时 | Python 标准库 | 3.10+ | 使用 `urllib`、`html.parser`、`hashlib`、`json` 完成低依赖采集与证据管理 |
| 本地存储 | JSON / JSONL / Markdown | schema v1 | 保持与现有本地优先销售工作区一致 |
| 页面 | 原生 HTML/CSS/JavaScript | ES2020 | 继续使用单文件销售作战台，不引入构建链 |
| 智能分析 | SpaceAgents 当前主模型 | 用户配置 | 用于主体消歧、网页理解、动态解释和销售回复 |
| 数据来源 | 官方公开网站 + 公开搜索结果 | 按来源实时 | 官方原文优先，搜索摘要只能作为待核验线索 |

## 数据文件

| 文件 | 所属 Phase | 用途 |
|------|-----------|------|
| `indexes/company-identity-index.json` | Phase 1 | 保存客户与企业法定主体的映射 |
| `indexes/company-intelligence.jsonl` | Phase 1 | 保存不可覆盖的外部动态证据流水 |
| `customers/<customer>/company-intelligence.md` | Phase 3 | 保存面向销售的企业动态摘要 |
| `.spaceagents/cache/sa-sales-advisor/company-radar/` | Phase 2 | 保存公开页面缓存、访问时间和失败状态，避免高频访问 |

## 开发规则

- 每完成一个 Phase 执行代码审查、自动化测试、编译验证和功能测试。
- 任何验证码、登录、403、429 或访问限制都必须停止自动采集并降级，不实现规避功能。
- 外部线索、已核验外部动态和销售确认事实分层保存，禁止跨层静默写入。
- 每个提交使用清晰的功能说明，不覆盖用户现有客户档案和原始材料。
